name: Workspace auth patch
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/workspace-auth-patch.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply auth patch
        run: python backend/scripts/workspace_auth_patch.py
      - name: Validate backend bundle
        working-directory: backend
        run: npm install --progress=false && npx wrangler deploy --dry-run --outdir /tmp/byggplan-worker
      - name: Commit result and cleanup
        run: |
          git config user.name "byggplan-bot"
          git config user.email "byggplan-bot@users.noreply.github.com"
          rm .github/workflows/workspace-auth-patch.yml backend/scripts/workspace_auth_patch.py
          git add backend/src/auth-routes.ts .github/workflows/workspace-auth-patch.yml backend/scripts/workspace_auth_patch.py
          git commit -m "Make authentication workspace aware"
          git push
